<div class="container row">
  <% if @unread %>
    <span>Unread</span>
  <% end %>
  <div class="col-10">
    <p><%= notification.body %></p>
  </div>
  <div class="col-2">
    <%= link_to "View", exam_path(notification.exam.id) %>
  </div>
</div>
